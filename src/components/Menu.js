import './Menu.css';
import React from 'react'

const Menu = (props) => {

    return (
        <div className='Menu'>
            <h1>APE.</h1>
            <h1>FILM / PHOTO / CLIENT / NOTE / CONTACT</h1>
        </div>
    )
}

export default Menu;